export interface AsanaDTO {
    asanaid: number; // Corresponds to Integer in Java
    asananame?: string; // Optional property, corresponds to String in Java
    url?: string; // Optional property, corresponds to String in Java
}